// const replies = ['🚂🌈💖', 'Choo choo!', 'Ding! 🛎', 'Never forget this dot!'];

export function chat(msg, args) {
    msg.reply('pls fish');
    // const index = Math.floor(Math.random() * replies.length);
    // msg.channel.send(replies[index]);
}